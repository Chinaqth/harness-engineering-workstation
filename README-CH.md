# Harness Engineering 工作基站

一个可以从个人实践起步，并逐步扩展到大型组织治理流程的 AI 工程控制平面。

[English README](README.md)

## 项目概括

这个仓库是团队与 Coding Agent 协作方式的系统记录。它通过 Git 管理工程规则、交付流程、决策边界、可复用 Skill、评估契约、验证证据和演进历史，把模型能力转化为稳定、可治理的工程能力。

工作基站不绑定具体模型、IDE 或供应商，也不追求编写一个“万能提示词”。它关注模型周围的完整工作环境，让 AI 工作具备以下特征：

- 目标、范围和上下文可以被发现；
- 权限和执行深度与风险匹配；
- 工作可以跨会话、跨人员继续；
- 系统行为可以被观察；
- 完成结论有独立、可复现的证据；
- 错误出现后能够回滚、恢复或升级；
- 每次交付都能沉淀为组织知识。

### 这个仓库是什么

- AI 辅助工程的组织级控制平面；
- 规则、红线、模板、Skill 和评估标准的版本化来源；
- 以制品为核心的 `3+1` 交付闭环参考实现；
- 企业项目逐步采纳 Harness Engineering 的起点。

### 这个仓库不是什么

- 不替代产品需求、业务架构、测试体系和人的最终责任；
- 不是通用提示词集合；
- 不是生产部署系统；
- 不是给 Agent 授予长期或无限权限的理由；
- 不能在没有真实项目证据时直接证明企业已经具备成熟能力。

## 为什么需要 Harness Engineering

Prompt Engineering 优化一次指令，Context Engineering 优化模型当前能看到什么，而 Harness Engineering 管理完整执行闭环：

```text
业务意图
  -> 选择上下文
  -> 评估风险
  -> 计划并建立契约
  -> 在预算内生成
  -> 观察系统行为
  -> 独立评估
  -> 批准、恢复或升级
  -> 将证据沉淀为组织知识
```

优化对象不再是模型的一次回答，而是整个“人、Agent、工具、规则和系统”共同组成的工作流可靠性。

## 工程思想

### 1. 仓库是持久记忆

聊天记录是临时界面。需求、决策、进度、证据和经验必须进入 Git 管理的制品，让另一个合格的执行者可以检查并继续工作。

### 2. 渐进式上下文优于超长指令

`AGENTS.md` 只承担导航职责。Agent 根据当前任务按需读取架构、治理、工作流、变更记录和深层参考资料。

### 3. 自治是一组风险预算

Agent 的权限应受到范围、工具、权限、外部副作用、时间、成本、证据和升级条件约束。风险越高，检查点越密集，职责分离越严格。

### 4. 验收必须可以执行

重要目标应拥有稳定的验收编号、机器可读状态、可复现检查和证据路径。不能因为输出看起来完整或 Agent 表示有信心就判定完成。

### 5. 生成和评估是不同职责

Generator 负责实现并提供证据；Evaluator 负责挑战证据。对于 G2/G3 工作，最终技术判定属于 Evaluator。`blocked` 不能被当作通过。

### 6. 可观测性是 Agent 的输入能力

健康检查、日志、链路、截图、结构化事件和可重复的用户旅程都属于 Agent 接口。无法观察的行为无法被可靠评估。

### 7. 每次交付都应改善工作基站

重复出现的问题应转化为规则、检查、测试、Skill 或架构指导。一次任务不仅要交付结果，还要降低下一次同类任务的不确定性。

## 运行模型

项目采用 `3+1` 生命周期：

| 阶段 | 目的 | 主要输出 |
| --- | --- | --- |
| 1. 计划 | 澄清意图、范围、风险、标准和权限 | 需求、任务、决策、自治预算 |
| 2. 实现 | 执行最小、可逆、已批准的工作单元 | 代码或文档、进度、验证证据 |
| 3. 评估与交付 | 重现关键行为并给出结论 | 验收状态、问题、回滚指导 |
| +1. 组织化沉淀 | 保存经验并改善未来工作 | 归档记录、规则、Skill、测试、指标 |

```text
Owner 提出意图
    |
Planner -> 需求 + 验收 + 契约
    |
Generator -> 实现 + 进度 + 证据
    |
Evaluator -> pass / fail / blocked
    |
Owner 批准 -> 交付或升级
    |
Archiver -> 持久知识和下一轮系统改进
```

详细规则参见 [3+1 工作流](workflows/3-plus-1.md)。

## 风险与自治等级

| 等级 | 典型工作 | 默认控制方式 |
| --- | --- | --- |
| G0 — 本地且可逆 | 文档、测试、安全重构 | Agent 可以本地执行和验证 |
| G1 — 有限影响 | 新依赖、公共接口、小型迁移 | 记录理由并接受人工评审 |
| G2 — 高影响 | 权限、安全边界、破坏性兼容变更 | 完整变更制品、独立评估、批准和回滚演练 |
| G3 — 不可逆或受监管 | 生产数据删除、敏感信息披露、重大合规变更 | 人主导执行、双人批准、审计记录 |

风险由影响范围、可逆性、数据敏感度和外部副作用共同决定。存在不确定性时向上取整。完整自治预算见 [AUTONOMY_POLICY.md](docs/AUTONOMY_POLICY.md)。

## 目录和文件职责

### 入口、规则和架构

| 路径 | 职责 | 何时阅读或更新 |
| --- | --- | --- |
| `README.md` | 英文项目总览和操作说明 | 英文使用者首次进入项目 |
| `README-CH.md` | 中文项目总览和操作说明 | 中文使用者首次进入项目 |
| `AGENTS.md` | Agent 的精简导航入口和强制协作规则 | 每次 Agent 开始工作 |
| `rules/CORE.md` | 组织级红线和验证规则 | 任务涉及安全、质量或权限时 |
| `docs/ARCHITECTURE.md` | 能力模型、信息分层和控制平面设计 | 修改结构或进行项目采纳时 |
| `docs/GOVERNANCE.md` | 风险等级、角色、审批、例外和指标 | 评估或批准重要变更时 |
| `docs/AUTONOMY_POLICY.md` | 范围、权限、成本、时间和升级预算 | 决定 Agent 可以自主执行什么时 |
| `docs/OBSERVABILITY.md` | 启动、执行、观察和重置系统的最小接口 | 让产品具备 Agent 可评估能力时 |
| `docs/MATURITY_MODEL.md` | L0–L4 能力等级和退出标准 | 审计采纳情况或规划改进时 |

### 工作执行和持久状态

| 路径 | 职责 | 主要维护者 |
| --- | --- | --- |
| `workflows/3-plus-1.md` | 计划、实现、评估、沉淀的标准生命周期 | 工作流 Owner |
| `changes/README.md` | 变更记录规则、状态和分级要求 | Planner |
| `changes/_template/requirements.md` | 问题、范围、验收、风险、预算和回滚 | Planner 与 Owner |
| `changes/_template/task.md` | 工作拆分、验证矩阵和最终判定 | Planner、Generator、Evaluator |
| `changes/_template/acceptance.json` | 机器可读验收状态与证据路径 | Generator 提议，Evaluator 验证 |
| `changes/_template/progress.md` | 跨会话交接和精确恢复点 | 当前执行者 |
| `changes/_template/contract.md` | Generator–Evaluator 边界和证据标准 | Planner 与 Evaluator |
| `changes/_template/decision.md` | 重要取舍、影响和重新评估条件 | 决策 Owner |
| `changes/archive/` | 已完成或已取消的变更历史 | Archiver |

### Skill、自动化与证据

| 路径 | 职责 |
| --- | --- |
| `skills/end-to-end-evaluator/` | 独立重现用户行为并给出有证据的判定 |
| `skills/harness-audit/` | 从七个 Harness Engineering 维度审计项目 |
| `scripts/harness-check.sh` | 运行完整工作基站检查 |
| `scripts/validate_change.py` | 验证变更制品与 `acceptance.json` 语义 |
| `scripts/knowledge-garden.py` | 检查失效链接和过期活动变更 |
| `tests/` | 验证检查器的正确和拒绝路径 |
| `.github/workflows/harness-check.yml` | 在推送和 Pull Request 时运行完整检查 |
| `.github/workflows/knowledge-garden.yml` | 定期检查知识新鲜度 |
| `.github/pull_request_template.md` | 在交付时要求范围、证据、判定、风险和回滚 |
| `docs/knowledge/` | 已整理、已分析的项目知识 |
| `docs/reference/` | 被保留的权威来源，不能被静默改写 |

## 变更制品

完整的 G2/G3 变更包含：

```text
changes/<YYYYMMDD-short-name>/
├── requirements.md   # 原因、范围、风险、验收、预算、回滚
├── task.md           # 执行计划、验证矩阵、最终判定
├── acceptance.json   # 稳定验收项、状态、证据
├── progress.md       # 当前状态和恢复点
├── contract.md       # Generator–Evaluator 契约
└── decision.md       # 决策取舍和后果
```

G0 可以记录在任务或 Pull Request 中。G1 需要 `requirements.md`、`task.md` 和 `progress.md`。风险越高，制品越完整。

## 快速开始

1. 阅读 [AGENTS.md](AGENTS.md)、[CORE.md](rules/CORE.md) 和当前项目上下文。
2. 将任务评估为 G0–G3。
3. 对于 G1 及以上任务，将 `changes/_template/` 复制到带日期的变更目录，并根据风险保留必要制品。
4. 在实现前定义可观察的验收标准。
5. 声明自治预算和人工审批点。
6. 每次只实现一个受控工作单元，并持续更新 `progress.md`。
7. 运行：

   ```bash
   ./scripts/harness-check.sh
   ```

8. 对重要的用户可见行为使用 `skills/end-to-end-evaluator`。
9. 记录最终判定、剩余风险和回滚方式。
10. 归档变更，并将稳定结论升级为规则、Skill、测试或架构知识。

## 多场景用例流程

### 场景 A：小型文档或安全重构 — G0

适用于本地、可逆且没有外部副作用的工作。

```text
声明目标和验收标准
  -> 完成最小改动
  -> 运行相关检查
  -> 审查差异
  -> 带验证证据提交
```

只需要任务或 Pull Request 记录。如果涉及公共契约、新依赖或大范围规范，应升级为 G1。

### 场景 B：功能、依赖或公共 API 变更 — G1

1. 创建 `requirements.md`、`task.md` 和 `progress.md`。
2. 记录必要性、兼容性、替代方案和回滚方式。
3. 定义可观察的用户或系统结果。
4. 以可验证的小步骤实现。
5. 运行自动检查和针对性评估。
6. 合并前接受人工评审。
7. 归档记录并更新持久指导。

### 场景 C：权限、安全或破坏性迁移 — G2

1. 创建完整的六项变更制品。
2. 明确 Owner、Planner、Generator 和 Evaluator。
3. 声明环境、账号、数据等级、工具权限、外部副作用、成本和检查点。
4. 建立失败基线或变更前不变量。
5. 每次只实现一个已批准、可回滚的任务单元。
6. 由 Evaluator 独立重现关键用户旅程。
7. 演练回滚；无法演练时必须说明原因和风险。
8. 合并、迁移、部署或修改权限前必须获得 Owner 批准。

### 场景 D：不可逆或受监管操作 — G3

Agent 可以检查、模拟、起草计划和收集证据，但状态变更由人主导执行。

```text
双人授权
  -> 固定范围和证据记录
  -> Dry Run 或模拟
  -> 执行前检查点
  -> 人工执行状态变更
  -> 独立确认结果
  -> 审计和恢复记录
```

缺少权限时必须升级，不能把“未说明”解释为“已授权”。

### 场景 E：长周期、跨会话任务

1. 将大目标拆分为可以独立验证的增量。
2. 保持 `acceptance.json` 中的验收描述稳定。
3. 每次暂停时，在 `progress.md` 记录当前版本、环境、证据、阻塞项和下一个最小安全动作。
4. 新会话从仓库状态和交接记录开始，而不是从聊天记忆中猜测。
5. 范围、架构或风险变化时重新规划。

### 场景 F：缺陷诊断和修复

```text
重现可观察故障
  -> 保存失败基线
  -> 找到被违反的不变量
  -> 实现最小修复
  -> 运行回归和边界检查
  -> 重新评估原始用户旅程
  -> 将失败模式沉淀为测试、规则或 Skill
```

如果任务只要求诊断，则诊断本身不代表获得修改授权。

### 场景 G：在现有产品仓库中采纳工作基站

1. 使用 `skills/harness-audit` 建立有证据的现状基线。
2. 添加简洁的项目级 `AGENTS.md`，指向项目权威资料。
3. 继承组织级红线，项目规则不得削弱它们。
4. 为产品关键旅程实现可观测性适配器。
5. 先试运行一个 G1 和一个 G2 变更，再全面强制执行。
6. 记录偏差、Owner、到期日和补偿控制。
7. 跟踪返工、逃逸缺陷、交付时间、评估可靠性和例外到期情况。

控制平面发布版本，产品仓库主动选择和固定版本。更新不能静默覆盖项目特定规则。

### 场景 H：将重复经验转化为 Skill 或规则

当一类任务拥有稳定触发条件、专业步骤、明确输入输出和验证方式时，适合创建 Skill。当组织必须强制某个不变量时，应创建规则或自动检查。

```text
重复任务或重复失败
  -> 提取稳定知识
  -> 选择文档、Skill、测试或规则
  -> 定义 Owner 和触发条件
  -> 使用真实案例验证
  -> 通过变更记录发布
  -> 衡量价值并清理过时内容
```

## 企业落地路径

建议渐进式采纳：

| 阶段 | 重点 | 进展证据 |
| --- | --- | --- |
| 1. 建立基线 | 入口、红线、变更模板、确定性检查 | 重要工作可见且可以复现 |
| 2. 建立治理 | 风险、Owner、审批、回滚、Skill 责任 | 高风险工作可以追踪 |
| 3. 建立度量 | 质量、周期、成本、返工、例外、评估可靠性 | 决策开始使用趋势数据 |
| 4. 自适应改进 | 动态上下文、工具选择、评估集、知识自动升级 | Harness 变更能改善真实指标 |

不要用提示词数量或生成代码行数评价个人。应衡量系统是否减少缺陷、歧义、返工、不安全操作和恢复成本。

## 项目验证

运行完整检查：

```bash
./scripts/harness-check.sh
```

它会验证必要文件、可疑凭据文件名、语言策略、变更记录语义、文档链接、活动变更新鲜度和检查器测试。

## 当前状态与下一步

当前基线为 `v0.2`。它已经建立 L2 治理所需的控制平面接口，但真正的企业就绪仍需要真实产品项目的采纳证据。

建议下一步选择一个正在开发的仓库进行试点：

- 一个 G1 功能或依赖变更；
- 一个 G2 安全、权限或兼容性变更；
- 一套项目专属可观测性适配器；
- 一段用于衡量返工、缺陷、交付时间和评估可靠性的观察周期。

设计依据可继续阅读 [成熟度模型](docs/MATURITY_MODEL.md) 和 [视频分析文档](docs/knowledge/harness-engineering-video-analysis-bv12lr1b3eut.md)。
